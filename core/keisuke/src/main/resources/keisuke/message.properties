# keisuke message(default) properties

# CountReport column label
count.language = LANG
count.executableSteps = CODE
count.blancSteps = BLANK
count.commentSteps = COMMENT
count.sumSteps = SUM
count.files = FILES
# CountReport subject
count.subject.head = [Each_language_files, Sum_steps, Code_steps, Comment_steps, Blank_steps]
count.subject.unsupport = [Unsupported_files]

# DiffReport column label
diff.language = LANG
diff.addSteps = ADD
diff.deleteSteps = DEL
diff.sumSteps = AMMOUNT
diff.files = FILES
# DiffReport subject
diff.subject.add = [ADDED Each_language_files, Added_steps]
diff.subject.modify = [MODIFIED Each_language_files, Modified_steps]
diff.subject.drop = [DROPED Each_language_files, Deleted_steps]
diff.subject.unchange = [UNCHANGED Each_language_files]
diff.subject.unsupport = [Unsupported_files]

#DiffCount status
diff.status.add = ADDED
diff.status.modify = MODIFIED
diff.status.drop = DROPED
diff.status.unchange = UNCHANGED
diff.status.unsupport = UNSUPPORTED

#StepCount format label
count.format.undef = UNSUPPORTED
count.format.path = FileName
count.format.type = Type
count.format.category = Category
count.format.execution = Code
count.format.blanc = Blank
count.format.comment = Comnt
count.format.sum = Sum
count.format.total = TOTAL

#DiffCount format label
diff.render.undef = UNSUPPORTED
diff.render.time = Running at
diff.render.path = Dir/File Name
diff.render.status = Status
diff.render.increase = Added Steps
diff.render.decrease = Deleted Steps
diff.render.html.title = Different Steps
diff.render.html.expand = Show All
diff.render.html.hide = Hide All
